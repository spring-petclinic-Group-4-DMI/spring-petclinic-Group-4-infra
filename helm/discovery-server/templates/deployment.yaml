apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "discovery-server.fullname" . }}
  labels:
    {{- include "discovery-server.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "discovery-server.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "discovery-server.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: discovery-server
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8761
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "discovery-server.fullname" . }}-config
