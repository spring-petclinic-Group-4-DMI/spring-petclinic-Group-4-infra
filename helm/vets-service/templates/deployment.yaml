apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vets-service.fullname" . }}
  labels:
    {{- include "vets-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "vets-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "vets-service.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: vets-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8083
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "vets-service.fullname" . }}-config
