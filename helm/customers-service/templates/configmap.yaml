apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "customers-service.fullname" . }}-config
  labels:
    {{- include "customers-service.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.configMap | nindent 2 }}
