apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: visits-service
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    # Sync wave 2 — depends on config-server, discovery-server, and (via FK)
    # customers-service. The wait_app pattern in the install workflow enforces
    # customers-service first; sync waves are coarser-grained.
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default

  sources:
    - repoURL: https://github.com/spring-petclinic-Group-4-DMI/spring-petclinic-Group-4-infra.git
      targetRevision: main
      ref: values
    - repoURL: https://github.com/spring-petclinic-Group-4-DMI/spring-petclinic-Group-4-infra.git
      targetRevision: main
      path: helm/petclinic-service
      helm:
        releaseName: visits-service
        valueFiles:
          - $values/helm-values/visits-service.yaml
          - $values/helm-values/dev.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: petclinic-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 2m
