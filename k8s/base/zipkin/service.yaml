apiVersion: v1
kind: Service
metadata:
  name: zipkin
  namespace: monitoring
  labels:
    app.kubernetes.io/name: zipkin
    app.kubernetes.io/part-of: petclinic
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9411
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: zipkin
