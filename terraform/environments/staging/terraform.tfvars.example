# Copy this file to terraform.tfvars and fill in real values
# NEVER commit terraform.tfvars — only commit this example file

environment        = "stg"
aws_region         = "us-east-1"
db_password        = "REPLACE_WITH_SECRET_FROM_SECRETS_MANAGER"
